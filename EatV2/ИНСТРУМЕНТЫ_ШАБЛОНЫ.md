# 🛠️ ИНСТРУМЕНТЫ ДЛЯ РАБОТЫ С ШАБЛОНАМИ

## 📦 НАБОР ИНСТРУМЕНТОВ:

---

### 1. `capture_templates.py` - 📸 Захват Screenshot

**Назначение:** Создает pixel-perfect скриншот игры через mss (как бот)

**Использование:**
```bash
python3 capture_templates.py
```

**Что делает:**
1. Ждет 5 секунд (подготовь игру!)
2. Захватывает `GAME_REGION` через Vision (mss)
3. Сохраняет `pixel_perfect_screenshot.png`

**Результат:**
- ✅ Скриншот ТОЧНО такого же размера как бот видит
- ✅ Нет Retina 2x проблемы
- ✅ Готов для вырезки шаблонов

**Когда использовать:**
- Создаешь новые шаблоны
- Пересоздаешь старые (были 2x Retina)
- Обновляешь шаблоны (изменился UI игры)

---

### 2. `check_templates.py` - 📏 Проверка Размеров

**Назначение:** Показывает размеры всех шаблонов в `assets/`

**Использование:**
```bash
python3 check_templates.py
```

**Что делает:**
1. Сканирует папку `assets/`
2. Загружает все `.png` файлы
3. Показывает размеры и статус
4. Выявляет проблемные шаблоны

**Результат:**
```
📏 РАЗМЕРЫ ШАБЛОНОВ:
upgrade_arrow        48×52      2496    ✅
btn_buy             120×125    15000    🔴 Retina 2x?

⚠️  ПРОБЛЕМНЫЕ ШАБЛОНЫ:
   - btn_buy: Размер 120×125 - возможно 2x Retina!
```

**Когда использовать:**
- После создания новых шаблонов
- Перед запуском диагностики
- Если бот не находит элементы

---

### 3. `debug_vision.py` - 🔍 Диагностика Распознавания

**Назначение:** Тестирует ВСЕ шаблоны на реальном скриншоте игры

**Использование:**
```bash
python3 debug_vision.py
```

**Что делает:**
1. Захватывает скриншот игры
2. Тестирует каждый шаблон на всех масштабах (0.5-2.0)
3. Показывает лучший confidence и scale
4. Сохраняет `debug_output.png` с прямоугольниками

**Результат:**
```
📦 upgrade_arrow.png
   📊 РЕЗУЛЬТАТЫ по масштабам:
      ✅ Scale 1.00: confidence 0.9543  ← ИДЕАЛЬНО!
   ✅ НАЙДЕНО! Лучший scale: 1.00

📊 АНАЛИЗ МАСШТАБОВ:
   Scale 1.00x: 10 шаблонов ← Pixel-perfect!
```

**Когда использовать:**
- После создания шаблонов (проверка)
- Если бот не находит элементы (диагностика)
- Перед запуском бота (финальная проверка)

---

### 4. `run.py` - 🤖 Запуск Бота

**Назначение:** Основной бот

**Использование:**
```bash
python3 run.py
```

**Что делает:**
- Все функции бота (upgrades, collect, navigate)

**Когда использовать:**
- После успешной диагностики
- Когда все шаблоны pixel-perfect (scale ≈ 1.0)

---

## 🔄 ТИПИЧНЫЙ WORKFLOW:

### Создание новых шаблонов:

```bash
# 1. Захват
python3 capture_templates.py
# → pixel_perfect_screenshot.png

# 2. Вырезка (в Preview/Photoshop)
open pixel_perfect_screenshot.png
# → Сохрани в assets/

# 3. Проверка размеров
python3 check_templates.py
# → Должны быть 30-120px

# 4. Диагностика
python3 debug_vision.py
# → Scale должен быть ≈ 1.0

# 5. Запуск
python3 run.py
```

### Исправление Retina 2x:

```bash
# 1. Проверка
python3 check_templates.py
# → btn_buy: 120×125 🔴 Retina 2x?

# 2. Диагностика
python3 debug_vision.py
# → Scale 0.50 ← Подтверждение Retina

# 3. Пересъемка
python3 capture_templates.py

# 4. Вырезка заново
open pixel_perfect_screenshot.png

# 5. Проверка
python3 check_templates.py
# → btn_buy: 60×62 ✅

# 6. Диагностика
python3 debug_vision.py
# → Scale 1.00 ✅ ИДЕАЛЬНО!
```

### Тонкая настройка порогов:

```bash
# 1. Диагностика
python3 debug_vision.py
# → upgrade_arrow: confidence 0.65 (порог 0.72)
# → 💡 Попробуй уменьшить порог до 0.59

# 2. Редактируй config.py
# THRESHOLDS = {"upgrade_arrow": 0.59}

# 3. Проверка в боте
python3 run.py
```

---

## 📊 СРАВНЕНИЕ ИНСТРУМЕНТОВ:

| Инструмент | Что делает | Когда | Результат |
|------------|------------|-------|-----------|
| `capture_templates.py` | Захват screenshot | Создание шаблонов | `pixel_perfect_screenshot.png` |
| `check_templates.py` | Проверка размеров | После вырезки | Список размеров + проблемы |
| `debug_vision.py` | Тест распознавания | Перед запуском | Confidence + Scale + `debug_output.png` |
| `run.py` | Запуск бота | После диагностики | Работающий бот |

---

## 🎯 КРИТЕРИИ КАЧЕСТВА:

### ✅ ИДЕАЛЬНЫЕ ШАБЛОНЫ:

**check_templates.py:**
```
upgrade_arrow        48×52      2496    ✅
```
- Размер 30-120px
- Нет предупреждений

**debug_vision.py:**
```
Scale 1.00: confidence 0.9543  ← Scale ≈ 1.0!
Confidence: 0.9543             ← > 0.80!
```
- Scale близко к 1.0 (0.9-1.1)
- Confidence > 0.80

**run.py:**
```
DEBUG| Vision: Best match: scale=1.00, confidence=0.95
🎯 Найдено 3 стрелок
✅ Апгрейд завершен
```
- Бот находит все элементы
- Нет ложных срабатываний

---

## ⚠️ ПРОБЛЕМНЫЕ ШАБЛОНЫ:

### 🔴 Retina 2x:

**check_templates.py:**
```
upgrade_arrow       120×125    15000    🔴 Retina 2x?
```

**debug_vision.py:**
```
Scale 0.50: confidence 0.85  ← Scale 0.5 = 2x!
```

**Решение:**
```bash
python3 capture_templates.py
# Вырежи заново из pixel_perfect_screenshot.png
```

### ⚠️ Низкий Confidence:

**debug_vision.py:**
```
Scale 1.00: confidence 0.45  ← Слишком низко!
```

**Решения:**
1. Пересними (элемент не на экране)
2. Вырежи точнее (меньше фона)
3. Уменьши порог в `config.py`

---

## 🚀 БЫСТРЫЙ СТАРТ:

### Если шаблоны уже есть:

```bash
# Проверка → Диагностика → Запуск
python3 check_templates.py
python3 debug_vision.py
python3 run.py
```

### Если создаешь новые:

```bash
# Захват → Вырезка → Проверка → Диагностика → Запуск
python3 capture_templates.py
open pixel_perfect_screenshot.png
# (Вырежи в Preview)
python3 check_templates.py
python3 debug_vision.py
python3 run.py
```

### Если Retina проблема:

```bash
# Пересъемка → Вырезка → Проверка → Диагностика → Запуск
python3 capture_templates.py
open pixel_perfect_screenshot.png
# (Вырежи заново)
python3 check_templates.py  # Должны быть меньше
python3 debug_vision.py     # Scale должен быть ≈ 1.0
python3 run.py
```

---

## 📚 ДОКУМЕНТАЦИЯ:

### Руководства:
- `WORKFLOW_ШАБЛОНЫ.md` - Полный процесс создания
- `КАК_СОЗДАТЬ_ШАБЛОНЫ.md` - Детальная инструкция
- `RETINA_FIX.md` - Про multi-scale и Retina

### Технические:
- `RETINA_THRESHOLDS_UPDATE.md` - Обновление порогов
- `THRESHOLDS_COMPARISON.md` - Таблица изменений

### Быстрые:
- `ЗАПУСТИ_ДИАГНОСТИКУ.md` - Краткая инструкция

---

## 💡 СОВЕТЫ:

### Для capture_templates.py:
- ✅ Подготовь экран: максимум элементов
- ✅ Открой меню станции (для btn_buy)
- ✅ Скроллни чтобы видеть всё

### Для вырезки:
- ✅ Используй Preview на Mac (быстро)
- ✅ Только элемент, минимум фона
- ✅ Не слишком мало (< 20px плохо)

### Для check_templates.py:
- ✅ Запускай после каждой вырезки
- ✅ Если > 100px → Retina! Переделай
- ✅ Если < 20px → Слишком мал

### Для debug_vision.py:
- ✅ Запускай перед run.py
- ✅ Scale должен быть ≈ 1.0
- ✅ Confidence должен быть > 0.80
- ✅ Открой debug_output.png (визуально)

---

## 🎉 ФИНАЛЬНЫЙ ЧЕКЛИСТ:

- [ ] Запустил `capture_templates.py`
- [ ] Открыл `pixel_perfect_screenshot.png`
- [ ] Вырезал все нужные элементы
- [ ] Сохранил в `assets/` с правильными именами
- [ ] Запустил `check_templates.py` → Размеры 30-120px ✅
- [ ] Запустил `debug_vision.py` → Scale ≈ 1.0, Confidence > 0.80 ✅
- [ ] Открыл `debug_output.png` → Все зеленые прямоугольники ✅
- [ ] Запустил `run.py` → Бот находит все элементы ✅

**ВСЁ ГОТОВО! 🚀**

---

## 🔧 TROUBLESHOOTING:

| Проблема | Инструмент | Решение |
|----------|------------|---------|
| Бот не находит элемент | `debug_vision.py` | Смотри confidence и scale |
| Шаблон 2x Retina | `check_templates.py` | Пересними через `capture_templates.py` |
| Низкий confidence | `debug_vision.py` | Уменьши порог в `config.py` |
| Ложные срабатывания | `run.py` логи | Увеличь порог или вырежи точнее |
| Неизвестная проблема | Все по порядку | `check` → `debug` → `run` |

---

## ✨ ИТОГ:

**4 ИНСТРУМЕНТА = ИДЕАЛЬНЫЕ ШАБЛОНЫ!**

```
capture_templates.py → pixel_perfect_screenshot.png
                    ↓
               (Вырезка)
                    ↓
               assets/*.png
                    ↓
check_templates.py → Размеры OK ✅
                    ↓
debug_vision.py    → Scale 1.0, Conf > 0.80 ✅
                    ↓
run.py             → Бот работает! 🎉
```

**ИСПОЛЬЗУЙ ПРАВИЛЬНЫЕ ИНСТРУМЕНТЫ! 🛠️**
