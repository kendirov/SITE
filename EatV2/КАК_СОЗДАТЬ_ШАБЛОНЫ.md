# 📸 КАК СОЗДАТЬ PIXEL-PERFECT ШАБЛОНЫ

## 🎯 ПРОБЛЕМА:

Если делать скриншоты через **Mac Screenshot (Cmd+Shift+4)**, они будут **2x размер** (Retina), а бот захватывает через `mss` в **1x размер** (logical pixels).

**Результат:** Размеры не совпадают → нужен multi-scale → потеря точности.

---

## ✅ РЕШЕНИЕ: Pixel-Perfect Capture

### Шаг 1: Захвати скриншот ЧЕРЕЗ БОТА

```bash
cd /Users/kendirov/App/EatV2
python3 capture_templates.py
```

**Что происходит:**
1. Скрипт ждет 5 секунд (подготовь игру!)
2. Делает скриншот через `Vision()` (mss) - ТОЧНО КАК БОТ
3. Сохраняет `pixel_perfect_screenshot.png`

**Результат:**
```
✅ ГОТОВО!
📁 Сохранен файл: pixel_perfect_screenshot.png
📏 Размер: 500x900 pixels (твой GAME_REGION)
```

---

### Шаг 2: Подготовь игру ПЕРЕД захватом

**Открой игру и убедись что на экране:**
- ✅ Стрелки улучшения (`upgrade_arrow`) - несколько штук
- ✅ Кнопка General Upgrades (`icon_upgrades`) - внизу справа
- ✅ Чаевые (`tip_coin`) - золотые монеты
- ✅ Коробки (`box_floor`) - на полу
- ✅ **Если возможно:** открой меню станции чтобы видеть `btn_buy`

**💡 СОВЕТ:** Скроллни так, чтобы на экране было МАКСИМУМ элементов!

---

### Шаг 3: Вырежи шаблоны из pixel_perfect_screenshot.png

#### Инструменты (выбери любой):

1. **Preview (Mac)** - самый простой:
   ```bash
   open pixel_perfect_screenshot.png
   ```
   - Tools → Rectangular Selection
   - Обведи элемент
   - Cmd+K (Crop) или Cmd+C → Cmd+N (New from Clipboard)
   - File → Export → PNG → Сохрани в `assets/`

2. **Photoshop:**
   - Rectangular Marquee Tool (M)
   - Crop (C)
   - File → Export → PNG

3. **GIMP (бесплатно):**
   - Rectangle Select Tool
   - Image → Crop to Selection
   - File → Export As → PNG

4. **Online (photopea.com):**
   - Загрузи `pixel_perfect_screenshot.png`
   - Crop Tool
   - File → Export as PNG

---

### Шаг 4: ПРАВИЛА ВЫРЕЗКИ

#### ✅ ПРАВИЛЬНО:

**upgrade_arrow (стрелка улучшения):**
```
┌─────────────┐
│   ↗         │  ← Только кружок со стрелкой
│  (○)        │     Никакого фона!
│             │     Примерно 40x40 - 60x60 pixels
└─────────────┘
```

**btn_buy (кнопка покупки):**
```
┌──────────────────┐
│   [  BUY  ]      │  ← Вся кнопка целиком
│                  │     С фоном кнопки
└──────────────────┘     Примерно 80x30 pixels
```

**tip_coin (монета чаевых):**
```
┌────────┐
│  💰    │  ← Только монета
│ ($)    │     Без фона
└────────┘     Примерно 30x30 pixels
```

#### ❌ НЕПРАВИЛЬНО:

```
┌──────────────────────────┐
│                          │
│     ↗                    │  ← Слишком много фона!
│    (○)                   │     Будет ложные срабатывания
│                          │
│                          │
└──────────────────────────┘
```

```
┌───┐
│ ↗ │  ← Слишком маленько!
└───┘     Потеря деталей
```

---

### Шаг 5: Сохрани правильно

**Имена файлов (ТОЧНО как в config.py):**
```
assets/
├── upgrade_arrow.png       ← Стрелка улучшения
├── btn_buy.png             ← Кнопка BUY
├── blue_button.png         ← Синяя кнопка (General Upgrades)
├── icon_upgrades.png       ← Иконка апгрейдов (повар/шеф)
├── btn_close_x.png         ← Крестик закрытия
├── btn_okay.png            ← Кнопка Okay
├── btn_open_level.png      ← Кнопка Open Level
├── btn_renovate.png        ← Молоток Renovate
├── btn_fly.png             ← Самолет Fly
├── btn_confirm_renovate.png← Confirm для Renovate
├── btn_fly_confirm.png     ← Confirm для Fly
├── tip_coin.png            ← Монета чаевых
└── box_floor.png           ← Коробка на полу
```

**Формат:**
- ✅ PNG (важно!)
- ✅ RGB/RGBA (цветной)
- ✅ Не сжатый

---

### Шаг 6: Проверь шаблоны

```bash
python3 debug_vision.py
```

**Что смотреть:**
```
📦 upgrade_arrow.png
   📊 РЕЗУЛЬТАТЫ по масштабам:
      ✅ Scale 1.00: confidence 0.9543  ← ОТЛИЧНО! (близко к 1.0)
      ✅ Scale 0.90: confidence 0.9234
      ✅ Scale 1.10: confidence 0.9156
   
   ✅ НАЙДЕНО!
      Лучший scale: 1.00  ← ИДЕАЛЬНО! (pixel-perfect)
      Confidence: 0.9543
```

**Если scale близко к 1.0 → ИДЕАЛЬНО! Шаблон точный размер! ✅**

---

## 🎯 ОЖИДАЕМЫЕ РАЗМЕРЫ:

Примерные размеры для элементов:

| Элемент | Примерный размер | Описание |
|---------|------------------|----------|
| `upgrade_arrow` | 40-60px | Кружок со стрелкой |
| `btn_buy` | 80-120px wide | Кнопка BUY |
| `blue_button` | 80-100px | Синяя кнопка |
| `icon_upgrades` | 40-60px | Иконка апгрейдов |
| `btn_close_x` | 20-30px | Крестик (маленький!) |
| `tip_coin` | 30-40px | Монета |
| `box_floor` | 40-60px | Коробка |
| `btn_renovate` | 50-70px | Молоток |
| `btn_fly` | 50-70px | Самолет |

---

## 🔧 ЕСЛИ ДИАГНОСТИКА ПОКАЗЫВАЕТ ПРОБЛЕМЫ:

### Проблема 1: Scale 0.5 (Retina)

```
Scale 0.50: confidence 0.8543  ← Шаблон всё еще 2x!
```

**Решение:**
- Ты вырезал из Mac Screenshot (Cmd+Shift+4)
- ПЕРЕДЕЛАЙ: вырежи из `pixel_perfect_screenshot.png`!

### Проблема 2: Низкий confidence (< 0.70)

```
Scale 1.00: confidence 0.5234  ← Слишком низко!
```

**Причины:**
1. Слишком много фона → вырежи точнее
2. Слишком маленький → захвати больше
3. Элемент изменяется (анимация) → захвати "средний" кадр
4. Разные состояния (pressed/unpressed) → нужно 2 шаблона

**Решение:**
- Пересними `pixel_perfect_screenshot.png`
- Вырежи заново, точнее

### Проблема 3: Не находит вообще

```
❌ НЕ НАЙДЕНО
```

**Причины:**
1. Элемента не было на экране при capture_templates.py
2. Неправильное имя файла
3. Элемент анимирован (постоянно меняется)

**Решение:**
- Запусти `capture_templates.py` снова
- Убедись что элемент виден
- Вырежи заново

---

## 📊 ПРЕИМУЩЕСТВА PIXEL-PERFECT:

### До (Mac Screenshot):

```
Template: 100x100 pixels (2x Retina)
Screen:    50x50 pixels  (1x mss)
→ Нужен scale 0.5
→ Потеря точности
→ Confidence: 0.65-0.75
```

### После (pixel_perfect_screenshot.png):

```
Template: 50x50 pixels (1x from mss)
Screen:   50x50 pixels (1x mss)
→ Scale 1.0 (точное совпадение!)
→ МАКСИМАЛЬНАЯ точность
→ Confidence: 0.90-0.98 ✅
```

---

## ✅ ЧЕКЛИСТ:

- [ ] Запустил `python3 capture_templates.py`
- [ ] Подготовил игру (все элементы на экране)
- [ ] Получил `pixel_perfect_screenshot.png`
- [ ] Открыл в редакторе
- [ ] Вырезал нужные элементы
- [ ] Сохранил в `assets/` с правильными именами
- [ ] Запустил `python3 debug_vision.py`
- [ ] Confidence > 0.80 и Scale ≈ 1.0 ✅
- [ ] Запустил `python3 run.py`
- [ ] Бот находит все элементы! 🎉

---

## 🚀 БЫСТРЫЙ СТАРТ:

```bash
# 1. Открой игру, подготовь экран
# 2. Захвати скриншот
python3 capture_templates.py

# 3. Открой результат
open pixel_perfect_screenshot.png

# 4. Вырежи элементы → сохрани в assets/

# 5. Проверь
python3 debug_vision.py

# 6. Если confidence > 0.80 и scale ≈ 1.0 → запускай бота!
python3 run.py
```

---

## 💡 СОВЕТЫ:

1. **Захватывай когда много элементов на экране** - меньше повторов
2. **Вырезай точно** - только элемент, минимум фона
3. **Сохраняй PNG** - не JPG! (потеря качества)
4. **Используй Preview на Mac** - быстро и просто
5. **Проверяй через debug_vision.py** - перед запуском бота

---

## 📚 СВЯЗАННЫЕ ФАЙЛЫ:

- `capture_templates.py` - Скрипт захвата
- `debug_vision.py` - Диагностика
- `pixel_perfect_screenshot.png` - Результат захвата
- `assets/` - Папка для шаблонов
- `config.py` - Пороги и настройки

**PIXEL-PERFECT ШАБЛОНЫ = ИДЕАЛЬНОЕ РАСПОЗНАВАНИЕ! ✨**
