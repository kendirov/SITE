# 🔧 ИСПРАВЛЕНИЕ: Бесконечное зацикливание скролла

## ❌ ПРОБЛЕМА:

Бот бесконечно скроллил вверх, MSE всегда был 700-1400 и никогда не падал ниже порога 100.

### Логи показывали:
```
📊 MSE после скролла вверх: 1241.6 (порог: 100)
⬆️  Продолжаю скроллить ВВЕРХ...
📊 MSE после скролла вверх: 958.1 (порог: 100)  
⬆️  Продолжаю скроллить ВВЕРХ...
📊 MSE после скролла вверх: 753.3 (порог: 100)
⬆️  Продолжаю скроллить ВВЕРХ...
[бесконечно...]
```

### Причина:
**АНИМАЦИИ В ИГРЕ!** 🏃‍♂️🚗

- Люди ходят
- Машины ездят  
- Фон меняется
- Огни мигают

Поэтому картинка **ВСЕГДА** меняется, даже когда экран упёрся в край!

MSE сравнивал ВСЮ картинку → всегда находил изменения → никогда не останавливался.

---

## ✅ РЕШЕНИЕ (3 исправления):

### 1. Ограничение количества попыток ⏱️

**Добавлено в config.py:**
```python
NAVIGATOR = {
    "MAX_SCROLL_UP": 6,      # Максимум 6 скроллов вверх
    "MAX_SCROLL_DOWN": 10,   # Максимум 10 скроллов вниз (уровень длиннее)
}
```

**Теперь:**
- Скроллим максимум 6 раз вверх
- Если не нашли верх за 6 попыток → считаем что нашли
- Защита от бесконечного цикла ✅

### 2. Сравнение только статичных зон 🎯

**Новый метод `_calculate_static_mse()`:**

```python
def _calculate_static_mse(img1, img2):
    # Берём только верхние 15% (UI элементы)
    top1 = img1[:static_h, :]
    top2 = img2[:static_h, :]
    
    # И нижние 15% (UI элементы)
    bottom1 = img1[-static_h:, :]
    bottom2 = img2[-static_h:, :]
    
    # Игнорируем центр (где анимации!)
    # Сравниваем только UI зоны
```

**Почему это работает:**
- UI элементы (верх/низ) не анимируются
- Центр экрана (где люди/машины) игнорируется
- MSE теперь показывает реальное движение экрана

### 3. Проверка стабилизации MSE 📊

**Добавлено в config.py:**
```python
NAVIGATOR = {
    "MSE_STABILITY_THRESHOLD": 200,  # Если MSE меняется < 200 → упёрлись
}
```

**Логика:**
```python
if self.last_mse is not None:
    mse_diff = abs(mse - self.last_mse)
    
    if mse_diff < 200:  # MSE почти не меняется?
        # Упёрлись! Картинка стабилизировалась
        return True
```

**Как работает:**
- Запоминаем предыдущий MSE
- Сравниваем с текущим
- Если изменение < 200 → экран не двигается → упёрлись!

---

## 📊 Новые логи:

### Теперь увидишь:
```
🧭 Navigator INIT_TOP: Скролл ВВЕРХ 1/6...
📊 MSE статичных зон: 450.2 (попытка 1/6)
⬆️  MSE изменился на 450.2 - продолжаю вверх...

🧭 Navigator INIT_TOP: Скролл ВВЕРХ 2/6...
📊 MSE статичных зон: 380.5 (попытка 2/6)
⬆️  MSE изменился на 69.7 - продолжаю вверх...

🧭 Navigator INIT_TOP: Скролл ВВЕРХ 3/6...
📊 MSE статичных зон: 395.1 (попытка 3/6)
🎯 MSE стабилизировался (изменение: 14.6) - ДОСТИГЛИ ВЕРХА!
```

Или если не стабилизировался:
```
🧭 Navigator INIT_TOP: Скролл ВВЕРХ 6/6...
🎯 Достигнут лимит скроллов (6) - считаем что наверху!
```

---

## ⚙️ Настройки (config.py):

```python
NAVIGATOR = {
    # Ограничения
    "MAX_SCROLL_UP": 6,              # Максимум скроллов вверх (4-6 достаточно)
    "MAX_SCROLL_DOWN": 10,           # Максимум скроллов вниз (уровень длиннее)
    
    # Статичные зоны
    "STATIC_ZONE_HEIGHT": 0.15,      # Сравниваем 15% верха и 15% низа
                                      # (только UI, без анимаций)
    
    # Стабилизация
    "MSE_STABILITY_THRESHOLD": 200,  # Если MSE меняется < 200 → упёрлись
}
```

### Тонкая настройка:

#### Хочешь больше/меньше скроллов?
```python
"MAX_SCROLL_UP": 4,   # Меньше (быстрее, но может пропустить верх)
"MAX_SCROLL_UP": 8,   # Больше (медленнее, но точнее)
```

#### Хочешь строже проверку стабилизации?
```python
"MSE_STABILITY_THRESHOLD": 100,  # Строже (остановится раньше)
"MSE_STABILITY_THRESHOLD": 300,  # Мягче (будет скроллить дольше)
```

#### Хочешь сравнивать больше/меньше UI?
```python
"STATIC_ZONE_HEIGHT": 0.10,  # Только 10% верха/низа (меньше UI)
"STATIC_ZONE_HEIGHT": 0.20,  # Целых 20% верха/низа (больше UI)
```

---

## 🎯 Как это работает теперь:

### Скролл ВВЕРХ (INIT_TOP):
1. ✅ Скролл 1: MSE = 450
2. ✅ Скролл 2: MSE = 380 (изменение 70 > 200) → продолжаем
3. ✅ Скролл 3: MSE = 395 (изменение 15 < 200) → **СТОП! Упёрлись!**

Или:
1. ✅ Скролл 1-6: MSE всё время меняется > 200
2. ✅ Скролл 6: **Лимит достигнут → СТОП!**

### Скролл ВНИЗ (SCAN_DOWN):
Та же логика:
- Максимум 10 шагов вниз
- Проверка стабилизации MSE
- Сравнение только статичных зон

---

## 🔍 Debug информация:

В логах теперь видно:
- **Попытка X/Y** - сколько скроллов сделано / максимум
- **MSE статичных зон** - только UI, без анимаций
- **Изменение MSE** - насколько MSE отличается от предыдущего
- **Причина остановки**:
  - "MSE стабилизировался" → экран не двигается
  - "Достигнут лимит" → сделали максимум скроллов

---

## ✅ Результат:

### Было:
- ❌ Бесконечное зацикливание
- ❌ MSE всегда 700-1400
- ❌ Никогда не останавливался
- ❌ Надо было жать ESC

### Стало:
- ✅ Максимум 6 скроллов вверх
- ✅ Максимум 10 скроллов вниз  
- ✅ Умная проверка стабилизации
- ✅ Сравнение только UI зон
- ✅ **РАБОТАЕТ!** 🎉

---

## 🚀 Запуск:

```bash
python3 run.py
```

Теперь увидишь:
```
🧭 Navigator INIT_TOP: Скролл ВВЕРХ 1/6...
📊 MSE статичных зон: 420.3 (попытка 1/6)
⬆️  MSE изменился на 420.3 - продолжаю вверх...

🧭 Navigator INIT_TOP: Скролл ВВЕРХ 2/6...
📊 MSE статичных зон: 390.1 (попытка 2/6)
⬆️  MSE изменился на 30.2 - продолжаю вверх...

🧭 Navigator INIT_TOP: Скролл ВВЕРХ 3/6...
📊 MSE статичных зон: 385.5 (попытка 3/6)
🎯 MSE стабилизировался (изменение: 4.6) - ДОСТИГЛИ ВЕРХА!

🔍 Navigator SCAN_DOWN: Пауза 1/3 - проверяю и апгрейжу...
[...нормальная работа...]
```

**Больше НЕ зацикливается! ✅**

---

## 💡 Почему это работает:

1. **Лимит попыток** - физическая защита от бесконечности
2. **Статичные зоны** - игнорируем анимации (люди, машины)
3. **Стабилизация MSE** - если MSE почти не меняется → упёрлись

Комбинация этих 3 методов **гарантирует** остановку в 4-6 скроллов!

---

**Готово! Запускай и больше не будет зацикливания! 🎉**
