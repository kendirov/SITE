# 🔧 Диагностика проблемы со скроллом

## Что я исправил:

### 1. Исправлен метод скролла в `core/input_manager.py`

**Было** (не работало):
```python
pyautogui.moveTo(screen_start_x, screen_start_y, duration=0.1)
pyautogui.mouseDown()
pyautogui.moveTo(screen_end_x, screen_end_y, duration=duration)  # ❌ Это не drag!
pyautogui.mouseUp()
```

**Стало** (должно работать):
```python
pyautogui.moveTo(screen_start_x, screen_start_y, duration=0.1)
time.sleep(0.1)
pyautogui.drag(drag_x, drag_y, duration=duration, button='left')  # ✅ Настоящий drag!
```

### 2. Добавлено детальное логирование

Теперь в терминале ты будешь видеть **ВСЁ**:

```
🧭 Navigator INIT: Ищу дно уровня...
⬇️  Скроллю ВНИЗ: (250, 630) → (250, 180) | Дистанция: 450px | Фактор: 100%
🔄 СКРОЛЛ: от (250, 630) → (250, 180) | Экран: (250, 630) → (250, 180) | Дистанция: -450px по Y
✅ Скролл выполнен, жду анимацию 0.8s
📊 MSE после скролла: 45.2 (порог: 100)
⬇️  Продолжаю скроллить вниз...
```

## Как протестировать:

### Вариант 1: Тестовый скрипт (рекомендую!)

1. Открой игру
2. Запусти:
```bash
python3 test_scroll.py
```

3. Скрипт протестирует **3 разных метода** скролла:
   - Метод 1: `pyautogui.drag()` (то что сейчас в боте)
   - Метод 2: `mouseDown + moveTo + mouseUp` (старый)
   - Метод 3: `pyautogui.scroll()` (колесико)

4. **Смотри какой сработал** и скажи мне номер!

### Вариант 2: Запусти бота с новым логированием

```bash
python3 run.py
```

Теперь в терминале будешь видеть:
- Когда бот скроллит
- Куда скроллит (координаты)
- Сколько пикселей
- Что он нашёл (стрелки, кнопки и т.д.)

## Возможные причины если не работает:

### 1. PyAutoGUI не может делать drag на твоей системе

**Решение**: Попробуем альтернативный метод через `pynput`:

```python
from pynput.mouse import Button, Controller

mouse = Controller()
mouse.position = (x1, y1)
mouse.press(Button.left)
time.sleep(0.1)
mouse.position = (x2, y2)
time.sleep(duration)
mouse.release(Button.left)
```

### 2. Приложение игры не реагирует на drag

Некоторые приложения (особенно эмуляторы Android/iOS) могут игнорировать стандартный drag.

**Решение**: Использовать AppleScript на macOS или специфичные методы для эмулятора.

### 3. Скорость drag'а слишком быстрая/медленная

**Решение**: Изменить `duration` в config.py:

```python
TIMERS["SCROLL_DURATION"] = 1.5  # Увеличь с 0.8 до 1.5
```

## Что делать дальше:

1. **Запусти test_scroll.py** и скажи мне какой метод сработал (1, 2 или 3)
2. **Если ни один не работает** - скажи мне, я сделаю версию через `pynput`
3. **Запусти бота** (`python3 run.py`) и покажи мне логи в терминале

## Про твою систему:

- **macOS**: pyautogui.drag должен работать
- **Bluestacks/эмулятор**: может не работать, нужен специальный метод
- **Нативное приложение iOS/Android**: может нужен ADB или другой метод

---

## 📋 Чеклист отладки:

- [ ] Запустил test_scroll.py
- [ ] Посмотрел какой метод работает
- [ ] Запустил run.py
- [ ] Вижу логи в терминале со всеми эмодзи
- [ ] Бот реально скроллит экран (не просто двигает мышку)

**Скажи мне результаты и я доделаю что нужно!**
