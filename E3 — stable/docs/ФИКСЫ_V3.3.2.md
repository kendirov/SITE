# EatventureBot V3.3.2 - Критичные Фиксы

**Дата:** 2026-02-03  
**Тип:** Bugfix Release

---

## 🐛 Исправленные Проблемы

### 1. ✅ Убран Лишний Клик При Свайпе

**Проблема:** 
При свайпе происходил лишний клик перед зажатием мыши:
```
click() → mouseDown() → move() → mouseUp()
```

Это вызывало нежелательные эффекты (открывались элементы интерфейса, случайные клики).

**Причина:**
В `core/input.py` метод `_drag_scroll` содержал:
```python
# STEP 1: Activate window first
pyautogui.click(screen_x1, screen_y1)  # ← Лишний клик!
time.sleep(0.2)

# STEP 2: Move to start
pyautogui.moveTo(screen_x1, screen_y1)

# STEP 3: Drag
pyautogui.drag(dx, dy)
```

**Решение:**
Убрали клик. Теперь только: **зажал → передвинул → отпустил**

```python
# STEP 1: Move to start position (no click!)
pyautogui.moveTo(screen_x1, screen_y1, duration=0.2)
time.sleep(0.15)

# STEP 2: Perform drag (press + move + release in one action)
pyautogui.drag(dx, dy, duration=duration, button='left')

# STEP 3: Pause to kill inertia
time.sleep(0.6)
```

**Результат:**
- ✅ Нет лишних кликов
- ✅ Чистый trackpad-like свайп
- ✅ Не открываются случайные элементы

---

### 2. 🔍 Улучшено Логирование Поиска Стрелок

**Проблема:**
Пользователь не мог понять почему бот "не ищет стрелочки улучшений":
- Логи были недостаточно подробными
- Не было видно что происходит на каждом этапе
- Невозможно было диагностировать проблему

**Решение:**
Добавлено детальное логирование на всех этапах:

#### В `core/logic.py` → `upgrade_stations()`:
```python
logger.debug("🔍 Ищем стрелки улучшений станций...")

if self.vision.zones_enabled:
    logger.debug(f"Зоны включены, ищем в безопасной зоне станций")
    arrows = self.vision.find_in_station_zone(...)
    logger.info(f"✓ Найдено {len(arrows)} стрелок улучшений в зоне Kitchen Floor")
else:
    logger.warning("⚠️  Зоны НЕ настроены! Ищем по всему экрану (не рекомендуется)")
    arrows = self.vision.find_template(...)
    logger.info(f"✓ Найдено {len(arrows)} стрелок улучшений (без безопасных зон)")

if not arrows:
    logger.debug("❌ Стрелки улучшений не найдены")
    return 0
```

#### В `core/vision.py` → `find_in_station_zone()`:
```python
logger.debug(f"🔍 find_in_station_zone: ищем '{template_name}' (find_all={find_all})")

if screenshot is None:
    logger.debug("  📸 Захватываем новый скриншот...")
else:
    logger.debug("  📸 Используем предоставленный скриншот")

logger.debug(f"  ✂️  Обрезаем до зоны станций: {self.station_search_region_relative}")
logger.debug(f"  ✂️  Размер обрезанного: {cropped.shape}, offset: ({offset_x}, {offset_y})")

logger.debug(f"  🔎 Ищем шаблон '{template_name}' в обрезанной зоне...")

if not results:
    logger.debug(f"  ❌ Шаблон '{template_name}' не найден")
else:
    logger.info(f"  ✓ Найдено {len(results)} '{template_name}' в зоне кухни")
    logger.debug(f"  ✓ Координаты после перевода: {adjusted}")
```

**Что Теперь Видно в Логах:**
```
[DEBUG] 🔍 Ищем стрелки улучшений станций...
[DEBUG] Зоны включены, ищем в безопасной зоне станций
[DEBUG] 🔍 find_in_station_zone: ищем 'upgrade_arrow' (find_all=True)
[DEBUG]   📸 Используем предоставленный скриншот
[DEBUG]   ✂️  Обрезаем до зоны станций: (12, 133, 315, 510)
[DEBUG]   ✂️  Размер обрезанного: (510, 315, 4), offset: (12, 133)
[DEBUG]   🔎 Ищем шаблон 'upgrade_arrow' в обрезанной зоне...
[DEBUG]   ❌ Шаблон 'upgrade_arrow' не найден
[INFO] ✓ Найдено 0 стрелок улучшений в зоне Kitchen Floor
[DEBUG] ❌ Стрелки улучшений не найдены
```

**Результат:**
- ✅ Видно каждый этап поиска
- ✅ Понятно где и как ищутся стрелки
- ✅ Легко диагностировать проблемы
- ✅ Эмодзи для быстрого поиска в логах

---

## 📊 Сравнение

| Аспект | До (V3.3.1) | После (V3.3.2) |
|--------|-------------|----------------|
| **Клик при свайпе** | Да (лишний) | Нет |
| **Свайп** | click → drag | moveTo → drag |
| **Логи поиска стрелок** | Минимальные | Детальные с эмодзи |
| **Диагностика** | Сложная | Простая |
| **Видно этапы** | Нет | Да |

---

## 🔍 Диагностика "Не Находит Стрелки"

Если бот не находит стрелки, смотрите логи:

### Сценарий 1: Зоны НЕ настроены
```
[WARNING] ⚠️  Зоны НЕ настроены! Ищем по всему экрану (не рекомендуется)
```

**Решение:**
```bash
python3 tools/setup_zones.py
```

### Сценарий 2: Зоны настроены, но стрелки не находятся
```
[DEBUG] Зоны включены, ищем в безопасной зоне станций
[DEBUG] 🔍 find_in_station_zone: ищем 'upgrade_arrow' (find_all=True)
[DEBUG]   ✂️  Размер обрезанного: (510, 315, 4)
[DEBUG]   ❌ Шаблон 'upgrade_arrow' не найден
```

**Возможные причины:**
1. Стрелки действительно отсутствуют на экране (все станции уже улучшены)
2. Неправильный шаблон `assets/upgrade_arrow.png`
3. Порог детекции слишком строгий (0.85)
4. Неправильно настроена зона (обрезается не то)

**Диагностика:**

#### Шаг 1: Захватить скриншот
```bash
python3 tools/capture_tool.py
```

Откройте `reference_screen.png` и проверьте:
- Видна ли игра?
- Видны ли стрелки на станциях?

#### Шаг 2: Проверить зону
В логах смотрите:
```
[DEBUG]   ✂️  Обрезаем до зоны станций: (12, 133, 315, 510)
```

Зона должна покрывать "кухню" где станции, исключая:
- Верхнее меню (деньги, гемы)
- Нижнее меню (синие кнопки)

Если зона неправильная:
```bash
python3 tools/setup_zones.py  # Перенастроить
```

#### Шаг 3: Проверить шаблон
Откройте `assets/upgrade_arrow.png`:
- Это именно та стрелка которая на станциях?
- Размер правильный (не слишком большой/маленький)?
- Качество хорошее (не размытая)?

Если шаблон неправильный:
1. `python3 tools/capture_tool.py`
2. Открыть `reference_screen.png`
3. Вырезать стрелку (точно как на экране)
4. Сохранить в `assets/upgrade_arrow.png`

#### Шаг 4: Порог детекции
В `config.py`:
```python
THRESHOLDS = {
    "upgrade_arrow": 0.85,  # Попробуйте 0.80 если не находит
}
```

---

## 🧪 Тестирование

### Тест 1: Свайпы Без Кликов
```bash
python3 run.py
# Наблюдайте за свайпами
```

**Ожидается:**
- ✅ Плавный свайп (без кликов)
- ✅ Не открываются случайные элементы
- ✅ Trackpad-like поведение

**Логи:**
```
[DEBUG] Moving to start position: screen(100, 300)
[DEBUG] Performing drag: distance=(0, -150), duration=0.5s
[DEBUG] ✓ Drag complete: 150px vertical movement
```

**НЕТ:**
```
[DEBUG] Activating window with click...  ← Этого больше нет!
```

### Тест 2: Логирование Поиска Стрелок
```bash
python3 run.py
# Дождитесь момента когда бот ищет стрелки
```

**Ожидается в логах:**
```
[DEBUG] 🔍 Ищем стрелки улучшений станций...
[DEBUG] Зоны включены, ищем в безопасной зоне станций
[DEBUG] 🔍 find_in_station_zone: ищем 'upgrade_arrow' (find_all=True)
[DEBUG]   📸 Используем предоставленный скриншот
[DEBUG]   ✂️  Обрезаем до зоны станций: (12, 133, 315, 510)
[DEBUG]   ✂️  Размер обрезанного: (510, 315, 4), offset: (12, 133)
[DEBUG]   🔎 Ищем шаблон 'upgrade_arrow' в обрезанной зоне...
```

**Результат:**
- ✅ Видны все этапы
- ✅ Эмодзи помогают найти нужное
- ✅ Понятно что происходит

---

## 📁 Измененные Файлы

### `core/input.py`
**Изменено:** Метод `_drag_scroll()`
- Убран: `pyautogui.click(screen_x1, screen_y1)`
- Теперь: только `moveTo → drag`

### `core/logic.py`
**Изменено:** Метод `upgrade_stations()`
- Добавлено детальное логирование
- Эмодзи для быстрого поиска
- Русские сообщения

### `core/vision.py`
**Изменено:** Метод `find_in_station_zone()`
- Добавлено пошаговое логирование
- Эмодзи для визуальной навигации
- Информация о размерах и offset

---

## ✅ Итого

**Версия:** V3.3.2  
**Тип:** Bugfix Release  
**Дата:** 2026-02-03

**Исправлено:**
1. ✅ Убран лишний клик при свайпе
2. ✅ Добавлено детальное логирование поиска стрелок

**Улучшено:**
- ✅ Чистые свайпы (trackpad-like)
- ✅ Диагностика проблем
- ✅ Понятные логи с эмодзи

**Статус:** ✅ Production Ready

---

## 🚀 Обновление

### Автоматически
```bash
git pull  # Или скачать новую версию
```

### Вручную
Обновить файлы:
- `core/input.py`
- `core/logic.py`
- `core/vision.py`

**Конфигурация НЕ требует изменений.**

---

## 📚 Дополнительно

- `README.md` - главная документация
- `СТРУКТУРА_ПРОЕКТА.md` - организация файлов
- `docs/ГОТОВО_V3.3.1_ИТОГОВЫЙ.txt` - предыдущий релиз

---

**Удачной автоматизации!** 🤖🍔✅
