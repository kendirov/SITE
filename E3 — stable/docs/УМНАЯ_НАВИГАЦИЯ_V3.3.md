# Умная Навигация V3.3.0

## 🎉 ЧТО ИСПРАВЛЕНО

### 1. ✅ Свайпы уменьшены в 2 раза
**Было:** 400px  
**Стало:** 200px (основные), 150px (сканирование)

### 2. ✅ Умная навигация с проверкой изменений

Вместо простого "долететь вниз" теперь:

#### **fly_to_top()** - Упираемся наверх
```python
def fly_to_top():
    """Летим наверх пока экран меняется"""
    
    for i in range(max_swipes):
        # Свайп вверх 200px (уменьшенная дистанция!)
        drag_screen("up", distance=200)
        
        # Сравниваем скриншоты
        if изменения_минимальны:
            # Достигли верха!
            break
```

**Логика:**
- Свайпаем вверх по 200px
- Сравниваем скриншоты до/после
- Если diff < 500000 = достигли потолка
- Учитывает динамику (люди/машины двигаются)

#### **scan_from_top_to_bottom()** - Медленно идем вниз
```python
def scan_from_top_to_bottom():
    """Проходимся сверху вниз, проверяя улучшения на каждом шаге"""
    
    for step in range(20):
        # 1. Проверяем улучшения на текущей позиции
        upgrades = upgrade_stations()
        
        # 2. Маленький шаг вниз (150px)
        drag_screen("down", distance=150)
        
        # 3. Проверяем достигли ли низа
        if экран_не_изменился:
            # Достигли низа!
            break
```

**Логика:**
- Делаем маленькие шаги вниз по 150px
- На каждом шаге проверяем улучшения
- Сравниваем скриншоты
- Если diff < 500000 = достигли низа
- Максимум 20 шагов (безопасность)

### 3. ✅ Учет динамических элементов

**Проблема:** Люди и машины двигаются, скриншоты всегда разные

**Решение:** Повышенный порог сравнения
- Было: diff < 100000 (слишком строго)
- Стало: diff < 500000 (учитывает динамику)

---

## 📊 Новая Логика Startup

### Старая (V3.2.1)
```
Step 1: Check General Upgrades
Step 2: Check Level Progression
Step 3: Fly to bottom (быстро вниз)
```

### Новая (V3.3.0)
```
Step 0: Activate window
Step 1: Check General Upgrades
Step 2: Check Level Progression
Step 3: Smart navigation:
  3.1. fly_to_top() - упираемся наверх
  3.2. scan_from_top_to_bottom() - медленно вниз, проверяя все
```

---

## 📊 Дистанции Свайпов

| Действие | Старая | Новая | Причина |
|----------|--------|-------|---------|
| Основной свайп | 400px | 200px | Слишком далеко |
| Сканирование | - | 150px | Маленькие шаги |
| Peek distance | 30% экрана | 200px | Консистентность |

---

## 🔄 Полное Сканирование (каждые 40 секунд)

### Старая логика peek_up_and_scan()
```
1. Свайп вверх на 30%
2. Проверить улучшения
3. Свайп вниз на 30%
4. Вернуться на место
```

### Новая логика (V3.3.0)
```
1. fly_to_top() - упираемся наверх
2. scan_from_top_to_bottom() - полное сканирование
3. Остаемся внизу (естественная позиция)
```

**Преимущества:**
- ✅ Проверяем ВСЕ улучшения, а не только часть
- ✅ Медленно проходим весь экран
- ✅ Не пропускаем ничего
- ✅ Учитываем динамику

---

## 🎯 Алгоритм Сравнения Скриншотов

```python
import cv2
import numpy as np

# Берем скриншот до свайпа
prev_screenshot = capture_screen()

# Свайпаем
drag_screen("up", distance=200)

# Берем скриншот после свайпа
new_screenshot = capture_screen()

# Сравниваем
diff = cv2.absdiff(prev_screenshot, new_screenshot)
diff_sum = np.sum(diff)

# Порог для "не изменился"
if diff_sum < 500000:
    print("Достигли края!")
else:
    print(f"Экран изменился (diff={diff_sum}), продолжаем...")
```

**Почему 500000?**
- Учитывает движение людей/машин
- Игнорирует мелкие анимации
- Но ловит реальные изменения контента

---

## 📝 Пример Работы

### Startup Sequence
```
[INFO] [STARTUP] Step 3: Smart navigation...
[INFO] 🔼 Летим наверх (ищем потолок)...
[DEBUG] Свайп 1: экран изменился (diff=1234567), продолжаем...
[DEBUG] Свайп 2: экран изменился (diff=987654), продолжаем...
[DEBUG] Свайп 3: экран изменился (diff=765432), продолжаем...
[INFO] ✓ Достигли верха после 4 свайпов (diff=345678)
[INFO] ✓ Наверху (выполнено 4 свайпов)

[INFO] 🔍 Сканируем сверху вниз (ищем улучшения)...
[DEBUG] Шаг 1/20: проверяем улучшения...
[INFO] Found 2 upgrade arrow(s) in Kitchen Floor
[INFO] ✓ Найдено 2 улучшений на шаге 1
[DEBUG] Шаг 2/20: проверяем улучшения...
[DEBUG] Шаг 3/20: проверяем улучшения...
...
[INFO] ✓ Достигли низа на шаге 8 (diff=234567)
[INFO] ✓ Сканирование завершено: найдено 5 улучшений
```

### Периодическое Сканирование (каждые 40 сек)
```
[INFO] 👀 ПОЛНОЕ СКАНИРОВАНИЕ: летим наверх и проверяем все...
[INFO] 🔼 Летим наверх (ищем потолок)...
[INFO] ✓ Достигли верха после 3 свайпов
[INFO] 🔍 Сканируем сверху вниз (ищем улучшения)...
[INFO] ✓ Найдено 2 улучшений на шаге 2
[INFO] ✓ Найдено 1 улучшений на шаге 5
[INFO] ✓ Достигли низа на шаге 7
[INFO] ✓ Полное сканирование: найдено 3 улучшений
[INFO] ✓ Полное сканирование завершено, остаемся внизу
```

---

## ✅ Что Улучшено

### Точность
- ✅ Маленькие шаги (150-200px вместо 400px)
- ✅ Проверяем каждую позицию
- ✅ Не пропускаем улучшения

### Надежность
- ✅ Учитываем динамику (люди/машины)
- ✅ Порог diff = 500000 (оптимальный)
- ✅ Безопасность: макс 15-20 шагов

### Эффективность
- ✅ Полное сканирование каждые 40 сек
- ✅ Не возвращаемся назад (остаемся внизу)
- ✅ Естественный цикл: верх → низ → верх → низ

---

## 🧪 Как Проверить

### Тест 1: Startup
```bash
python3 run.py
```

**Ожидается:**
```
[STARTUP] Step 3: Smart navigation...
🔼 Летим наверх (ищем потолок)...
✓ Достигли верха после X свайпов
🔍 Сканируем сверху вниз (ищем улучшения)...
✓ Сканирование завершено: найдено X улучшений
```

### Тест 2: Периодическое Сканирование
```bash
# Запустите бота, подождите 40 секунд
```

**Ожидается:**
```
👀 ПОЛНОЕ СКАНИРОВАНИЕ: летим наверх и проверяем все...
🔼 Летим наверх...
✓ Достигли верха
🔍 Сканируем сверху вниз...
✓ Полное сканирование завершено
```

### Тест 3: Визуальная Проверка
- ✅ Свайпы меньше (в 2 раза)
- ✅ Бот проходит весь экран медленно
- ✅ Останавливается наверху/внизу
- ✅ Находит все улучшения

---

## 🔧 Настройки

Если нужно изменить поведение:

### В `core/logic.py`:

**Дистанция свайпов:**
```python
# fly_to_top
self.input.drag_screen("up", distance=200)  # Измените здесь

# scan_from_top_to_bottom
self.input.drag_screen("down", distance=150)  # И здесь
```

**Порог сравнения:**
```python
if diff_sum < 500000:  # Увеличьте если слишком чувствительно
```

**Количество шагов:**
```python
max_steps = 20  # Увеличьте для более тщательного сканирования
```

---

## 📊 Сравнение Версий

| Параметр | V3.2.1 | V3.3.0 |
|----------|--------|--------|
| **Дистанция свайпа** | 400px | 200px |
| **Логика навигации** | Быстро вниз | Умная: верх→низ |
| **Проверка изменений** | Простая | С учетом динамики |
| **Сканирование** | Peek 30% | Полное верх→низ |
| **Периодичность** | 40 сек | 40 сек |
| **Пропуски** | Возможны | Минимальны |

---

## 🎯 Итого

**Версия:** 3.3.0  
**Дата:** 2026-02-03  
**Критичные изменения:** 2

**Что исправлено:**
1. ✅ Свайпы уменьшены в 2 раза (200px/150px)
2. ✅ Умная навигация: упираемся наверх → медленно вниз
3. ✅ Учет динамики (люди/машины)
4. ✅ Полное сканирование каждые 40 сек

**Бот готов к использованию!** 🚀✅
