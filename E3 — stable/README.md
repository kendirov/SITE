# EatventureBot V3.3.1 🤖🍔

**Высокопроизводительный бот для автоматизации игры Eatventure**  
Оптимизирован для macOS Retina дисплеев с iPhone Video Replay (трансляция видеоповтор)

---

## 🎯 Основные Возможности

- ✅ **Умная Навигация** - детекция краев экрана с учетом динамических элементов
- ✅ **Безопасные Зоны** - предотвращение случайных кликов по рекламе
- ✅ **Пространственная Память** - не кликает повторно по одному месту
- ✅ **Native Resolution** - работа в 1:1 пикселях без масштабирования
- ✅ **Модульная Архитектура** - vision, input, logic, state
- ✅ **Жесткая Остановка** - ESC останавливает немедленно

---

## 🚀 Быстрый Старт

### 1. Установка Зависимостей
```bash
cd /Users/kendirov/App/E3
pip3 install -r requirements.txt
```

**Зависимости:**
- `opencv-python` - компьютерное зрение
- `mss` - захват экрана
- `pyautogui` - управление мышью/клавиатурой
- `pynput` - детекция нажатий клавиш

### 2. Настройка Безопасных Зон (ВАЖНО!)
```bash
python3 tools/setup_zones.py
```

**Интерактивный мастер попросит вас указать:**
1. **Игровое Окно** - верхний левый и нижний правый угол
2. **Безопасная Зона** - область где находятся станции (исключая UI)
3. **Опасная Зона** - центр кнопки "Burger/Investor" (чтобы не кликать)

**Результат:** Автоматически создается `config.py` с вашими координатами.

### 3. Запуск Бота
```bash
python3 run.py
```

**Для остановки:** Нажмите **ESC** (работает ВСЕГДА!)

---

## 📁 Структура Проекта

```
E3/
├── run.py              # 🚀 Главный файл запуска
├── config.py           # ⚙️  Конфигурация (генерируется setup_zones.py)
├── requirements.txt    # 📦 Python зависимости
├── README.md           # 📖 Этот файл
├── СТРУКТУРА_ПРОЕКТА.md # 📋 Детальное описание структуры
│
├── core/               # 🧠 Ядро бота
│   ├── vision.py       # 👁️  Компьютерное зрение (OpenCV)
│   ├── input.py        # 🖱️  Управление мышью/клавиатурой
│   ├── logic.py        # 🎮 Игровая логика
│   └── state.py        # 💾 Пространственная память
│
├── tools/              # 🛠️  Утилиты
│   ├── setup_zones.py  # Настройка безопасных зон
│   └── capture_tool.py # Захват скриншотов для дебага
│
├── assets/             # 🎨 Игровые ресурсы (кнопки, иконки)
│   ├── upgrade_arrow.png
│   ├── btn_buy.png
│   ├── icon_upgrades.png
│   └── ...
│
├── docs/               # 📚 Документация и гайды
│   ├── QUICKSTART.md
│   ├── ГОТОВО_V3.3.1.txt
│   └── ... (30+ файлов)
│
└── archive/            # 📦 Архив (бэкапы, тесты)
    ├── config.py.backup.*
    └── test_swipe.py
```

**Подробнее:** Читайте `СТРУКТУРА_ПРОЕКТА.md`

---

## 🎮 Что Делает Бот

### Startup (Начальная Последовательность)
1. **Активация Окна** - кликает по центру игры (для macOS)
2. **Общие Улучшения** - проверяет синие кнопки
3. **Прогресс Уровня** - ищет кнопки Renovate/Fly
4. **Умная Навигация** - летит наверх, сканирует вниз

### Main Loop (Основной Цикл)
1. **Safety** - закрывает рекламу если появилась
2. **Общие Улучшения** - каждые 5 циклов (~30 сек)
3. **Прогресс Уровня** - проверка Renovate/Fly
4. **Улучшение Станций** - ищет `upgrade_arrow`, кликает
5. **Сбор Предметов** - монеты на столиках

### Periodic Scan (Каждые 40 сек)
- Полное сканирование: наверх → вниз
- Поиск пропущенных улучшений

---

## 🧠 Умная Навигация (V3.3.1)

### Как Работает Детекция Края

**Проблема:** В игре движутся люди, машины (20-30% динамики). Как понять что упёрлись в край?

**Решение:** Сравнение скриншотов по проценту изменений:
```python
# 1. Скриншот ДО свайпа
prev_screenshot = capture()

# 2. Свайп
swipe_up/down()
time.sleep(0.5)  # Ждем остановки инерции

# 3. Скриншот ПОСЛЕ
new_screenshot = capture()

# 4. Сравнение
change_percent = calculate_diff(prev, new)

# 5. Детекция края
if change_percent < 15%:
    # Учитывая 20-30% динамики,
    # если < 15% = значит контент не изменился
    # = упёрлись в край!
    stuck_count += 1
    
    if stuck_count >= 2:
        # 2 свайпа подряд < 15% = точно упёрлись
        STOP!
```

**Параметры:**
- **Порог:** < 15% изменений = край
- **Подтверждение:** 2 свайпа подряд < 15%
- **Макс попыток:** 10 (наверх), 12 (вниз)
- **Шаг:** 150px (вверх), 120px (вниз)

**Логирование:**
```
🔼 Летим наверх (умная детекция края)...
[DEBUG] Свайп 1/10: изменилось 45.23% экрана
[DEBUG]   ✓ Двигаемся (45.23% изменений)
[DEBUG] Свайп 2/10: изменилось 38.15% экрана
[DEBUG]   ✓ Двигаемся (38.15% изменений)
[DEBUG] Свайп 3/10: изменилось 12.34% экрана
[DEBUG]   ⚠️  Мало изменений (12.34%), stuck_count=1
[DEBUG] Свайп 4/10: изменилось 11.87% экрана
[DEBUG]   ⚠️  Мало изменений (11.87%), stuck_count=2
[INFO] ✓ УПЁРЛИСЬ В ВЕРХ после 4 свайпов (изменений: 11.87%)
```

---

## 🛡️ Безопасные Зоны

### Концепция
1. **GAME_REGION** - вся игровая область (x, y, width, height)
2. **STATION_SEARCH_REGION** - только "кухня" (где станции)
3. **DANGER_ZONE_CENTER** - центр кнопки "Burger/Investor"
4. **DANGER_RADIUS** - 60px (не кликать ближе)

### Как Это Работает
```python
# 1. Поиск стрелки ТОЛЬКО в безопасной зоне
arrows = vision.find_in_station_zone("upgrade_arrow")

# 2. Расчет точки клика (offset вниз-вправо)
click_x = arrow_x + 20
click_y = arrow_y + 60

# 3. Проверка безопасности
distance = euclidean(click_point, DANGER_ZONE_CENTER)

if distance < 60px:
    # Слишком близко к рекламе - НЕ КЛИКАТЬ!
    logger.warning("⚠️  Rejected: too close to Burger button")
    add_to_ignore_list()
else:
    # Безопасно - кликаем
    click(click_x, click_y)
```

**Результат:** Бот НИКОГДА не кликает по рекламе, даже если стрелка прямо над ней.

---

## 🔧 Конфигурация

### config.py
Автоматически генерируется через `python3 tools/setup_zones.py`.

**Основные параметры:**
```python
# Зоны (генерируются setup_zones.py)
GAME_REGION = (23, 38, 349, 748)  # Игровое окно
STATION_SEARCH_REGION = (35, 171, 315, 510)  # Безопасная зона
DANGER_ZONE_CENTER = (52, 663)  # Кнопка рекламы
DANGER_RADIUS = 60  # Безопасное расстояние

# Пороги детекции (0.0 - 1.0)
THRESHOLDS = {
    "upgrade_arrow": 0.85,  # Стрелка над станцией
    "btn_buy": 0.93,        # Кнопка покупки (строго!)
    "blue_button": 0.85,    # Общие улучшения
}

# Тайминги
TIMERS = {
    "STATION_MEMORY": 20.0,     # Не кликать повторно 20 сек
    "BUY_LONG_PRESS": 3.0,      # Долгое нажатие = купить много
    "MAIN_LOOP_DELAY": 0.1,     # Задержка основного цикла
}

# Клики
INPUT_CONFIG = {
    "CLICK_JITTER": 3,  # ±3px случайного смещения (humanization)
}
```

**Ручное редактирование:** Можно, но лучше перезапустить `setup_zones.py`.

---

## 🐛 Дебаг и Диагностика

### Проблема: Бот Кликает Не Там
```bash
# 1. Сделать скриншот
python3 tools/capture_tool.py

# 2. Откроется reference_screen.png
# 3. Проверить что бот видит правильную область
# 4. Если нет - перезапустить setup_zones.py
```

### Проблема: Не Находит Кнопку
```bash
# 1. Сделать скриншот игры через capture_tool.py
# 2. Открыть в редакторе
# 3. Вырезать кнопку (точно как на экране)
# 4. Сохранить в assets/my_button.png
# 5. Добавить в config.py:
#    ASSETS["my_button"] = "my_button.png"
#    THRESHOLDS["my_button"] = 0.85
```

### Проблема: Бот Зависает
- **ESC не работает?** Нажмите несколько раз, используйте `os._exit(0)`
- **Зависает на краях?** Проверьте логи - должно быть "УПЁРЛИСЬ В ВЕРХ/НИЗ"
- **Свайпы не двигаются?** Проверьте что `time.sleep(0.6)` после `drag()`

### Логи
```bash
# Включить DEBUG логи
# В config.py изменить:
LOG_LEVEL = "DEBUG"  # Было: "INFO"

# Теперь будет видно:
# - Процент изменений экрана
# - Координаты кликов (game + screen)
# - Детали каждого свайпа
```

---

## 📊 Производительность

### Типичный Цикл
```
Main Loop: ~0.5-1 сек
  - Vision (детекция): 100-200ms
  - Input (клик): 50-150ms
  - Logic (решения): <10ms
  - Delays (тайминги): 150-500ms

Full Scan (наверх + вниз): ~30-60 сек
  - fly_to_top: 5-15 сек (зависит от контента)
  - scan_from_top_to_bottom: 20-45 сек (проверка улучшений)
```

### Оптимизация
- ✅ Используется `mss` (быстрее чем PIL)
- ✅ Детекция только в `STATION_SEARCH_REGION` (не весь экран)
- ✅ Spatial Memory предотвращает повторные проверки
- ✅ Умная навигация останавливается при достижении края

---

## ⚠️ Важные Замечания

### macOS Specific
1. **Accessibility Permissions** - разрешите Python управлять компьютером
   - System Preferences → Security → Privacy → Accessibility
   - Добавьте Terminal.app или Python

2. **Screen Recording** - разрешите захват экрана
   - System Preferences → Security → Privacy → Screen Recording
   - Добавьте Terminal.app или Python

3. **iPhone Video Replay** - бот оптимизирован для трансляции видеоповтор
   - Свайпы эмулируют trackpad (`pyautogui.drag`)
   - Долгие паузы после свайпов (0.6с) для остановки инерции

### Координаты
- **Game-Relative** - координаты внутри `GAME_REGION` (0, 0) = верхний левый угол игры
- **Screen-Absolute** - координаты экрана, используются PyAutoGUI
- **Перевод:** `input.translate_to_screen(game_x, game_y)`

### Безопасность
- ✅ Никогда не кликает ближе 60px от `DANGER_ZONE_CENTER`
- ✅ Строгий порог для `btn_buy` (0.93) чтобы не перепутать с рекламой
- ✅ Проверка на `btn_ad_play` - если видим, НЕ кликаем

---

## 📚 Дополнительная Документация

- `СТРУКТУРА_ПРОЕКТА.md` - детальное описание файлов и папок
- `docs/QUICKSTART.md` - быстрый старт для новичков
- `docs/SETUP.md` - подробная инструкция по настройке
- `docs/ГОТОВО_V3.3.1.txt` - последние изменения (русский)
- `docs/КРИТИЧНЫЕ_ИСПРАВЛЕНИЯ_V3.3.1.md` - критичные фиксы
- `docs/УМНАЯ_НАВИГАЦИЯ_V3.3.md` - про умную навигацию
- `assets/README.md` - описание игровых ресурсов

---

## 🔄 Changelog

### V3.3.1 (2026-02-03)
**Критичные исправления:**
- ✅ ESC теперь жестко останавливает (`os._exit(0)`)
- ✅ Инерция свайпов убита (пауза 0.6с после drag)
- ✅ Умная навигация с детекцией динамических элементов
- ✅ Процентное сравнение скриншотов (< 15% = край)
- ✅ Детальное логирование процента изменений
- ✅ Организованная структура проекта (docs/, archive/)

### V3.3.0 (2026-02-02)
- ✅ Умная навигация "wall detection"
- ✅ Scan from top to bottom
- ✅ Уменьшенные дистанции свайпов

### V3.2.1 (2026-02-01)
- ✅ Trackpad emulation для macOS + iPhone
- ✅ Coordinate translation (game → screen)
- ✅ Window activation перед действиями

### V3.2.0 (2026-01-31)
- ✅ Безопасные зоны (Safe Zoning)
- ✅ Danger zone detection
- ✅ Spatial memory

### V3.0.0 (2026-01-30)
- ✅ Native Resolution подход
- ✅ Модульная архитектура
- ✅ OpenCV template matching

---

## 🤝 Поддержка

### Проблемы?
1. Проверьте `docs/ГОТОВО_V3.3.1.txt` - возможно уже исправлено
2. Перезапустите `setup_zones.py` - возможно координаты сбились
3. Сделайте `capture_tool.py` - проверьте что бот видит

### Вопросы?
- Читайте `СТРУКТУРА_ПРОЕКТА.md` - описание всех файлов
- Смотрите логи - там подробные детали
- Включите DEBUG уровень для детального анализа

---

## 📝 Лицензия

Проект создан для образовательных целей.

---

## 🎯 Статус

**Версия:** V3.3.1  
**Дата:** 2026-02-03  
**Статус:** ✅ PRODUCTION READY  

**Тестирование:**
- ✅ ESC работает (жесткий выход)
- ✅ Свайпы точные (без инерции)
- ✅ Умная навигация (детекция краев)
- ✅ Безопасные зоны (не кликает рекламу)
- ✅ Spatial memory (не спамит)

**Готово к использованию!** 🚀

---

**Удачной автоматизации!** 🤖🍔
