# 🚀 Инструкция по установке и запуску

## ⚠️ Важно!
Из-за кириллицы в пути проекта, автоматическая установка через Cursor может работать некорректно. 
Используйте терминал Windows вручную.

## Шаг 1: Откройте обычный терминал (CMD или PowerShell)

Нажмите `Win + R`, введите `cmd` и нажмите Enter.

## Шаг 2: Перейдите в директорию проекта

```cmd
cd "c:\Users\kendi\Yandex.Disk\SITE\Сайт"
```

## Шаг 3: Установите зависимости

```cmd
npm install
```

## Шаг 4: Установите дополнительный пакет для анимаций

```cmd
npm install tailwindcss-animate
```

## Шаг 5: Создайте .env файл

Скопируйте `.env.example` в `.env`:

```cmd
copy .env.example .env
```

Затем откройте `.env` и добавьте ваши API ключи:

```env
VITE_MOEX_API_BASE_URL=https://iss.moex.com
VITE_MOEX_API_KEY=your_api_key_here
VITE_MOEX_API_SECRET=your_api_secret_here
```

## Шаг 6: Запустите dev сервер

```cmd
npm run dev
```

Откройте браузер по адресу: `http://localhost:3000`

---

## 🎨 Быстрый старт (альтернатива)

Если у вас уже установлен Node.js, просто скопируйте эти команды в терминал:

```cmd
cd "c:\Users\kendi\Yandex.Disk\SITE\Сайт"
npm install
npm install tailwindcss-animate
copy .env.example .env
npm run dev
```

---

## 📦 Список установленных пакетов

### Основные зависимости
- `react` (18.3.1) - React библиотека
- `react-dom` (18.3.1) - React DOM
- `react-router-dom` (6.22.0) - Роутинг
- `zustand` (4.5.0) - State management
- `@tanstack/react-query` (5.22.0) - Data fetching
- `axios` (1.6.7) - HTTP client
- `recharts` (2.12.0) - Графики
- `lucide-react` (0.323.0) - Иконки
- `tailwind-merge` & `clsx` - Утилиты для CSS

### Dev зависимости
- `vite` (5.1.0) - Build tool
- `typescript` (5.3.3) - TypeScript
- `tailwindcss` (3.4.1) - CSS framework
- `@vitejs/plugin-react-swc` (3.6.0) - React plugin

---

## 🔥 Готово!

После установки и запуска вы увидите:

✅ Скринер акций на главной странице  
✅ Скринер фьючерсов  
✅ Академию с базой знаний  
✅ Dark Magic дизайн с glassmorphism  
✅ Переключение темы (светлая/темная)  
✅ Адаптивный дизайн  

---

## 🐛 Проблемы?

Если что-то не работает:

1. Убедитесь, что Node.js установлен: `node --version`
2. Очистите кэш: `npm cache clean --force`
3. Удалите `node_modules` и переустановите: 
   ```cmd
   rmdir /s /q node_modules
   npm install
   ```

---

**Happy Coding! 🚀✨**
