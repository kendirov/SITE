# Проблема Свайпов - Видеоповтор iPhone

## 🚨 Ситуация

**Окружение:**
- MacBook
- Видеоповтор iPhone (трансляция экрана iPhone на MacBook)
- Игра запущена на iPhone, видна через видеоповтор

**Проблема:**
- Свайпы должны работать как на трекпаде
- Левая кнопка мыши "дергано" работает
- Кажется, что кнопка не зажимается

## 🔍 Анализ

### Что происходит сейчас (V3.2.1)

```python
# Текущий код
pyautogui.mouseDown()  # Зажать
time.sleep(0.2)
# ... движение ...
time.sleep(0.2)
pyautogui.mouseUp()  # Отпустить
```

**Проблема:** Отдельные команды mouseDown/mouseUp могут работать как отдельные клики, а не как единый drag.

### Что нужно для видеоповтора iPhone

Видеоповтор iPhone реагирует на **непрерывное движение с зажатой кнопкой**, как на трекпаде.

## ✅ Решение V3.2.1.1

### Метод 1: pyautogui.drag() (НОВЫЙ - ПОПРОБУЙТЕ ПЕРВЫМ)

```python
def _drag_scroll(self, x1, y1, x2, y2, duration=0.5):
    # Активация окна
    pyautogui.click(screen_x1, screen_y1)
    time.sleep(0.2)
    
    # Навести курсор
    pyautogui.moveTo(screen_x1, screen_y1, duration=0.2)
    time.sleep(0.15)
    
    # Выполнить drag (КЛЮЧЕВОЕ ИЗМЕНЕНИЕ!)
    # drag() делает: зажать + двигать + отпустить в одном действии
    dx = screen_x2 - screen_x1
    dy = screen_y2 - screen_y1
    pyautogui.drag(dx, dy, duration=duration, button='left')
    
    time.sleep(0.2)
```

**Почему это должно работать:**
- `drag()` - это атомарная операция (одно непрерывное действие)
- Эмулирует настоящий трекпад
- Используется параметр `button='left'` явно

### Метод 2: pyautogui.dragTo() (альтернатива)

```python
# Если drag() не работает, попробуйте dragTo()
pyautogui.dragTo(screen_x2, screen_y2, duration=duration, button='left')
```

### Метод 3: Очень медленный drag (если ничего не работает)

```python
# Максимально медленный drag с множеством шагов
pyautogui.mouseDown(button='left')
time.sleep(0.3)

steps = 50  # Много шагов для плавности
for i in range(steps):
    t = i / steps
    current_y = start_y + (end_y - start_y) * t
    pyautogui.moveTo(center_x, int(current_y), duration=0.02)

time.sleep(0.3)
pyautogui.mouseUp(button='left')
```

## 🧪 Тест Свайпов

Создан тестовый скрипт `test_swipe.py` который пробует все 3 метода.

**Запуск:**
```bash
cd /Users/kendirov/App/E3
python3 test_swipe.py
```

**Что делает:**
1. Дает 5 секунд переключиться на игру
2. Тестирует ТЕСТ 1: `drag()`
3. Пауза 2 секунды
4. Тестирует ТЕСТ 2: `dragTo()`
5. Пауза 2 секунды
6. Тестирует ТЕСТ 3: медленный drag

**Что проверить:**
- Двигается ли контент после каждого теста?
- Какой тест работает лучше всего?

## 📁 Что изменено в V3.2.1.1

### `core/input.py`

**Было (V3.2.1):**
```python
pyautogui.mouseDown()
# ... движение по шагам ...
pyautogui.mouseUp()
```

**Стало (V3.2.1.1):**
```python
# Метод 1: Используем drag() как единое действие
pyautogui.drag(dx, dy, duration=duration, button='left')
```

## 🎯 Следующие Шаги

1. **Запустите тест:**
   ```bash
   python3 test_swipe.py
   ```

2. **Проверьте результаты:**
   - Какой тест работает? (1, 2, 3 или никакой)
   - Двигается ли контент?

3. **Сообщите результат:**
   - Если работает ТЕСТ 1 → отлично, уже в коде!
   - Если работает ТЕСТ 2 → нужно изменить на `dragTo()`
   - Если работает ТЕСТ 3 → нужно увеличить шаги
   - Если ничего не работает → возможно проблема с доступом

## 💡 Возможные Проблемы

### Доступ к видеоповтору

macOS может блокировать доступ к видеоповтору iPhone.

**Проверьте:**
```
Настройки → Конфиденциальность и безопасность → Доступ → 
→ Разрешить Terminal/Python управлять компьютером
```

### Видеоповтор неактивен

Первый клик может просто активировать окно.

**Решение:** Кликните в игру вручную перед запуском.

### QuickTime Player vs Finder

Если используете QuickTime Player для видеоповтора:
- Проверьте что окно активно
- Попробуйте полноэкранный режим

## 📊 Сравнение Методов

| Метод | Скорость | Надежность | Рекомендация |
|-------|----------|------------|--------------|
| `drag()` | Быстро | Высокая | ⭐ Попробуйте первым |
| `dragTo()` | Быстро | Средняя | Если drag() не работает |
| Медленный | Медленно | Очень высокая | Последняя попытка |

## ✅ Контрольный Список

**Перед тестом:**
- [ ] Видеоповтор iPhone активен
- [ ] Игра видна на экране MacBook
- [ ] Окно игры не минимизировано
- [ ] Доступ разрешен в настройках

**После теста:**
- [ ] Запущен test_swipe.py
- [ ] Проверены все 3 метода
- [ ] Записан результат (какой работает)
- [ ] Готов сообщить результат

## 🚀 После Теста

Когда узнаем какой метод работает, обновлю код бота соответственно.

**Версия:** 3.2.1.1  
**Дата:** 2026-02-03  
**Статус:** Тестирование методов свайпа
