# Структура Проекта EatventureBot V3.3.1

## 📁 Организация Файлов

Проект организован для удобства работы - все важные файлы в корне, дополнительные материалы в отдельных папках.

```
E3/
├── 🚀 ГЛАВНЫЕ ФАЙЛЫ (для запуска и настройки)
│   ├── run.py              # Главный файл запуска бота
│   ├── config.py           # Конфигурация (зоны, пороги, тайминги)
│   ├── requirements.txt    # Зависимости Python
│   └── README.md           # Главная документация
│
├── 📂 core/                # Ядро бота (НЕ ТРОГАТЬ без необходимости)
│   ├── vision.py           # Компьютерное зрение (детекция)
│   ├── input.py            # Управление мышью/клавиатурой
│   ├── logic.py            # Игровая логика (мозг бота)
│   └── state.py            # Пространственная память
│
├── 🛠️ tools/               # Утилиты и инструменты
│   ├── setup_zones.py      # Настройка безопасных зон
│   └── capture_tool.py     # Захват скриншотов для дебага
│
├── 🎨 assets/              # Игровые ресурсы (кнопки, иконки)
│   ├── upgrade_arrow.png   # Стрелка улучшения станции
│   ├── btn_buy.png         # Кнопка покупки
│   ├── icon_upgrades.png   # Иконка общих улучшений
│   └── README.md           # Описание ресурсов
│
├── 📚 docs/                # Документация и гайды
│   ├── QUICKSTART.md       # Быстрый старт
│   ├── SETUP.md            # Подробная настройка
│   ├── ГОТОВО_V3.3.1.txt   # Последние изменения
│   └── ... (другие гайды)
│
└── 📦 archive/             # Архив (старые версии, тесты)
    ├── config.py.backup.*  # Бэкапы конфигурации
    ├── test_swipe.py       # Тестовый скрипт свайпов
    └── verify_assets.py    # Проверка ресурсов
```

---

## 🎯 Как Использовать

### Запуск Бота
```bash
# Находитесь в главной папке E3/
python3 run.py
```

### Первичная Настройка
```bash
# Настройка безопасных зон (только один раз)
python3 tools/setup_zones.py
```

### Дебаг и Диагностика
```bash
# Сделать скриншот для проверки
python3 tools/capture_tool.py
```

---

## 📋 Описание Папок

### 🚀 Корневая Папка
**Содержит:** Только самые важные файлы для запуска и настройки.

**Файлы:**
- `run.py` - точка входа, запуск бота
- `config.py` - все настройки (генерируется `setup_zones.py`)
- `requirements.txt` - Python зависимости
- `README.md` - главная документация
- `.gitignore` - игнорируемые файлы для git

**Принцип:** Минимализм. Если файл не нужен для запуска - он в подпапке.

---

### 📂 core/
**Содержит:** Основной код бота (модульная архитектура).

**Модули:**
- `vision.py` - компьютерное зрение (OpenCV)
  - Захват экрана (mss)
  - Поиск шаблонов (template matching)
  - Зональная детекция (безопасные области)

- `input.py` - управление вводом
  - Клики с jitter (humanization)
  - Умные свайпы (trackpad emulation)
  - Перевод координат (game → screen)

- `logic.py` - игровая логика
  - Улучшение станций
  - Общие апгрейды
  - Навигация (умный скролл)
  - Детекция прогресса уровня

- `state.py` - состояние бота
  - Пространственная память (где кликали)
  - Предотвращение повторных кликов
  - Статистика

**Принцип:** Не редактировать без необходимости. Изменения делать только если понимаете архитектуру.

---

### 🛠️ tools/
**Содержит:** Вспомогательные утилиты и инструменты.

**Утилиты:**
- `setup_zones.py` - Интерактивный мастер настройки
  - Определение игровой области (GAME_REGION)
  - Настройка зоны станций (STATION_SEARCH_REGION)
  - Маркировка опасной зоны (DANGER_ZONE_CENTER)
  - Автоматическая генерация `config.py`

- `capture_tool.py` - Инструмент захвата скриншотов
  - Делает скриншот той же области, что видит бот
  - Сохраняет как `reference_screen.png`
  - Используется для дебага детекции

**Принцип:** Запускаются отдельно, когда нужно настроить или продебажить бота.

---

### 🎨 assets/
**Содержит:** Графические ресурсы (шаблоны для детекции).

**Типы ресурсов:**
1. **Триггеры** (открывают меню):
   - `upgrade_arrow.png` - стрелка над станцией
   - `icon_upgrades.png` - иконка общих улучшений

2. **Действия** (кнопки для клика):
   - `btn_buy.png` - кнопка покупки в меню станции
   - `blue_button.png` - кнопка в меню общих улучшений

3. **Коллекция**:
   - `tip_coin.png` - монеты на столиках

**Принцип:** Все PNG должны быть в native resolution (1:1, без масштабирования).

**Как добавить новый ресурс:**
1. Используйте `python3 tools/capture_tool.py` для захвата экрана
2. Откройте `reference_screen.png` в редакторе
3. Вырежьте нужный элемент (кнопку/иконку)
4. Сохраните в `assets/` с понятным именем
5. Добавьте в `config.py` → `ASSETS` и `THRESHOLDS`

---

### 📚 docs/
**Содержит:** Документация, гайды, changelog, заметки.

**Основные документы:**
- `QUICKSTART.md` - быстрый старт для новичков
- `SETUP.md` - детальная инструкция по настройке
- `ГОТОВО_V3.3.1.txt` - последние изменения (русский)
- `КРИТИЧНЫЕ_ИСПРАВЛЕНИЯ_V3.3.1.md` - критичные фиксы
- `ARCHITECTURE.md` - описание архитектуры
- `CHANGELOG.md` - история изменений

**Принцип:** Справочные материалы. Читать при необходимости, но не нужны для запуска.

---

### 📦 archive/
**Содержит:** Старые версии, бэкапы, тестовые скрипты.

**Что здесь:**
- `config.py.backup.*` - бэкапы конфигурации (создаются `setup_zones.py`)
- `test_swipe.py` - тестовый скрипт для проверки свайпов
- `verify_assets.py` - проверка наличия всех ресурсов

**Принцип:** Архив. Можно удалить без последствий, но лучше хранить на случай отката.

---

## 🔄 Workflow (Типичный Рабочий Процесс)

### 1. Первый Запуск
```bash
# 1. Установить зависимости
pip3 install -r requirements.txt

# 2. Настроить зоны (один раз)
python3 tools/setup_zones.py

# 3. Запустить бота
python3 run.py
```

### 2. Дебаг Детекции
```bash
# 1. Сделать скриншот
python3 tools/capture_tool.py

# 2. Открыть reference_screen.png
# 3. Проверить что бот видит правильную область
# 4. Если нет - перезапустить setup_zones.py
```

### 3. Добавление Нового Ресурса
```bash
# 1. Захватить экран
python3 tools/capture_tool.py

# 2. Вырезать нужный элемент из reference_screen.png
# 3. Сохранить в assets/new_button.png

# 4. Добавить в config.py:
# ASSETS["new_button"] = "new_button.png"
# THRESHOLDS["new_button"] = 0.85

# 5. Использовать в коде:
# pos = vision.find_template("new_button")
```

### 4. Обновление Конфигурации
```bash
# 1. Редактировать config.py вручную
# ИЛИ
# 2. Перезапустить setup_zones.py (создаст новый config.py)
```

---

## 🧹 Правила Чистоты Кода

### ✅ ХОРОШО
- Главная папка содержит только: `run.py`, `config.py`, `requirements.txt`, `README.md`
- Документация в `docs/`
- Бэкапы и тесты в `archive/`
- Модули в соответствующих папках (`core/`, `tools/`)

### ❌ ПЛОХО
- Множество `.md` файлов в корне
- Тестовые скрипты в главной папке
- Бэкапы конфигурации в корне
- Временные файлы не удалены

---

## 📊 Размеры и Статистика

```
Главная папка:     4 файла   (только самое важное)
core/:             4 модуля  (ядро бота)
tools/:            2 утилиты (настройка и дебаг)
assets/:           7 ресурсов (шаблоны для детекции)
docs/:            ~30 файлов (вся документация)
archive/:          3+ файла  (старые версии)
```

**Итого:** Чистая главная папка + организованная структура.

---

## 🎓 Для Новичков

**Что нужно знать для использования:**
1. Запуск: `python3 run.py`
2. Остановка: нажать `ESC`
3. Настройка зон: `python3 tools/setup_zones.py` (один раз)

**Что НЕ нужно трогать:**
- Папка `core/` (ядро бота)
- Файлы в `archive/` (бэкапы)

**Что МОЖНО трогать:**
- `config.py` - настройки (но лучше через `setup_zones.py`)
- `assets/` - добавлять новые шаблоны
- `docs/` - читать документацию

---

## 🔧 Для Разработчиков

**Архитектура:**
- Модульная (vision, input, logic, state)
- Строго типизированная (Python type hints)
- Без глобальных переменных (кроме config)
- Координаты: game-relative → screen-absolute (input.py)

**Стиль кода:**
- PEP 8
- Docstrings для всех функций
- Логирование вместо print
- ANSI цвета в логах для читаемости

**Тестирование:**
- Ручное (визуальное)
- Утилиты в `tools/` для дебага
- Логи с подробностями (DEBUG level)

---

## 📝 Changelog Структуры

**V3.3.1 (2026-02-03):**
- ✅ Создана организованная структура папок
- ✅ Главная папка очищена (только 4 файла)
- ✅ Документация перенесена в `docs/`
- ✅ Бэкапы и тесты в `archive/`
- ✅ Создан этот файл (СТРУКТУРА_ПРОЕКТА.md)

**До V3.3.1:**
- ❌ 30+ файлов в корне
- ❌ Документация вперемешку с кодом
- ❌ Бэкапы в главной папке
- ❌ Сложно найти нужное

---

## 🎯 Итого

**Принцип организации:** "Главная папка - это рабочий стол. Держим его чистым."

**Что в главной папке:**
- Что нужно для запуска (run.py, config.py, requirements.txt)
- Что нужно прочитать первым (README.md)
- Этот файл (СТРУКТУРА_ПРОЕКТА.md)

**Все остальное - в подпапках!**

---

Версия: V3.3.1  
Дата: 2026-02-03  
Автор: EatventureBot Team  
Статус: Актуально ✅
